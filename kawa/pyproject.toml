[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kawa"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydash>=8.0.5",
    "ruff>=0.14.6",
    "ty>=0.0.1a28",
    "pytest>=9.0.1",
]
